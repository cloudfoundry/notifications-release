networks: (( merge ))
jobs: (( merge ))

resource_pools:
- name: default
  network: notifications
  size: (( auto ))
  stemcell:
    name: bosh-warden-boshlite-ubuntu-trusty-go_agent
    version: latest
  cloud_properties:
    name: random
