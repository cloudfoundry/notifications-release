<%=
    require 'json'
    db = link('notifications_db').p('notifications.database')
    JSON.pretty_generate(
      'username' => db['username'],
      'password' => db['password'],
      'host' => db['host'],
      'port' => db['port'],
      'database' => db['database'],
      "adapter":"mysql",
    )
%>
